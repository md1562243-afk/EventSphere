import React from 'react';
import RegisterForm from './RegisterForm';

export default function RegisterUser() {
  return <RegisterForm role="User" title="Create a User Account" subtitle="Registration is instant — start booking right away" />;
}
